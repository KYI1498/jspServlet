전체 소스 코드 내용

1. 최종 프로젝트 구조

먼저 전체 구조를 잡겠습니다.

project2/
│
├── pom.xml
│
├── src/
│   └── main/
│       │
│       ├── java/
│       │   └── com/
│       │       └── mobility.git/
│       │           └── project2/
│       │               │
│       │               ├── controller/
│       │               │   └── BoardController.java
│       │               │
│       │               ├── dao/
│       │               │   └── BoardDAO.java
│       │               │
│       │               ├── dto/
│       │               │   └── BoardDTO.java
│       │               │
│       │               └── util/
│       │                   └── DBUtil.java
│       │
│       ├── resources/
│       │   └── db.properties
│       │
│       └── webapp/
│           │
│           ├── index.jsp
│           │
│           ├── css/
│           │   └── board.css
│           │
│           └── WEB-INF/
│               │
│               ├── web.xml
│               ├── lib
│               │        └── postgresql-42.7.13.jar
│               │
│               └── views/
│                   └── board/
│                       ├── list.jsp
│                       ├── detail.jsp
│                       ├── write.jsp
│                       └── edit.jsp
│
└── target/


MVC 관점으로 보면:

브라우저
   │
   │ HTTP 요청
   ▼
BoardController
   │
   ├───────────────┐
   ▼               ▼
BoardDAO        BoardDTO
   │
   ▼
PostgreSQL
   │
   ▼
BoardController
   │
   ▼
JSP
   │
   ▼
브라우저

이것이 이번에 구현할 MVC Level 2 구조입니다.




2. PostgreSQL 테이블부터 만들기

먼저 PostgreSQL에서 데이터베이스를 만듭니다.

예를 들어:

CREATE DATABASE app;

그리고 app에 접속해서 다음 SQL을 실행합니다.

CREATE TABLE board (
    id          BIGSERIAL PRIMARY KEY,
    title       VARCHAR(200) NOT NULL,
    content     TEXT NOT NULL,
    writer      VARCHAR(100) NOT NULL,
    created_at  TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at  TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
);

테스트 데이터도 몇 개 넣겠습니다.

INSERT INTO board (title, content, writer)
VALUES
('첫 번째 게시글', '게시판에 오신 것을 환영합니다.', 'admin'),
('두 번째 게시글', 'JSP/Servlet MVC 게시판입니다.', 'admin'),
('세 번째 게시글', 'PostgreSQL과 연동되었습니다.', 'user01');

확인:

SELECT * FROM board ORDER BY id DESC;



3. pom.xml

먼저 프로젝트 루트의 pom.xml입니다.

<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="
            http://maven.apache.org/POM/4.0.0
            https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.example</groupId>
    <artifactId>board-mvc</artifactId>
    <version>1.0-SNAPSHOT</version>

    <!-- WAR 파일 생성 -->
    <packaging>war</packaging>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>

        <!-- Jakarta Servlet 6.0 -->
        <!-- Tomcat이 제공하므로 WAR에는 포함하지 않음 -->
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <version>6.0.0</version>
            <scope>provided</scope>
        </dependency>

        <!-- Jakarta JSP 3.1 -->
        <!-- Tomcat이 제공 -->
        <dependency>
            <groupId>jakarta.servlet.jsp</groupId>
            <artifactId>jakarta.servlet.jsp-api</artifactId>
            <version>3.1.1</version>
            <scope>provided</scope>
        </dependency>

        <!-- JSTL API -->
        <dependency>
            <groupId>jakarta.servlet.jsp.jstl</groupId>
            <artifactId>jakarta.servlet.jsp.jstl-api</artifactId>
            <version>3.0.2</version>
        </dependency>

        <!-- JSTL 구현체 -->
        <dependency>
            <groupId>org.glassfish.web</groupId>
            <artifactId>jakarta.servlet.jsp.jstl</artifactId>
            <version>3.0.1</version>
        </dependency>

        <!-- PostgreSQL JDBC Driver -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>42.7.13</version>
        </dependency>

    </dependencies>

    <build>

        <finalName>board-mvc</finalName>

        <plugins>

            <!-- Java 21 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.1</version>

                <configuration>
                    <release>21</release>
                </configuration>
            </plugin>

            <!-- WAR -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.4.0</version>
            </plugin>

        </plugins>

    </build>

</project>

Tomcat 10.1은 Servlet 6.0 / Pages 3.1을 구현하므로 이 프로젝트에서는 javax.servlet.*가 아니라 jakarta.servlet.*를 사용합니다.




4. DB 접속 정보

파일:

src/main/resources/db.properties

내용:

db.url=jdbc:postgresql://localhost:5432/app
db.username=postgres
db.password=여기에_본인의_PostgreSQL_비밀번호

예를 들어 PostgreSQL 설치 때 비밀번호가 1234였다면:

db.url=jdbc:postgresql://localhost:5432/app
db.username=postgres
db.password=1234

입니다.

실제 프로젝트에서는 비밀번호를 소스 저장소에 그대로 올리지 않는 것이 좋지만, 지금은 학습용 프로젝트이므로 구조를 단순하게 유지하겠습니다.




5. DBUtil.java

위의 db.properties를 읽어서 PostgreSQL Connection을 만들어주는 클래스입니다.

파일:

src/main/java/com/mobility/git/project2/util/DBUtil.java
package com.mobility.git.project2.util;

import java.io.IOException;
import java.io.InputStream;
import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;
import java.util.Properties;

public class DBUtil {

    private static final String URL;
    private static final String USERNAME;
    private static final String PASSWORD;

    static {
        Properties properties = new Properties();

        try (InputStream inputStream =
                     DBUtil.class.getClassLoader()
                             .getResourceAsStream("db.properties")) {

            if (inputStream == null) {
                throw new RuntimeException("db.properties 파일을 찾을 수 없습니다.");
            }

            properties.load(inputStream);

            URL = properties.getProperty("db.url");
            USERNAME = properties.getProperty("db.username");
            PASSWORD = properties.getProperty("db.password");

        } catch (IOException e) {
            throw new RuntimeException("DB 설정 파일을 읽을 수 없습니다.", e);
        }
    }

    private DBUtil() {
    }

    public static Connection getConnection() throws SQLException {
        return DriverManager.getConnection(
                URL,
                USERNAME,
                PASSWORD
        );
    }
}

PostgreSQL JDBC Driver가 Maven dependency로 포함되어 있기 때문에 DriverManager를 통해 연결할 수 있습니다.




6. BoardDTO.java

게시글 하나를 표현하는 DTO입니다.

파일:

src/main/java/com/mobility/git/project2/dto/BoardDTO.java
package com.mobility.git.project2.dto;

import java.time.LocalDateTime;

public class BoardDTO {

    private long id;
    private String title;
    private String content;
    private String writer;
    private LocalDateTime createdAt;
    private LocalDateTime updatedAt;

    public BoardDTO() {
    }

    public BoardDTO(
            long id,
            String title,
            String content,
            String writer,
            LocalDateTime createdAt,
            LocalDateTime updatedAt
    ) {
        this.id = id;
        this.title = title;
        this.content = content;
        this.writer = writer;
        this.createdAt = createdAt;
        this.updatedAt = updatedAt;
    }

    public long getId() {
        return id;
    }

    public void setId(long id) {
        this.id = id;
    }

    public String getTitle() {
        return title;
    }

    public void setTitle(String title) {
        this.title = title;
    }

    public String getContent() {
        return content;
    }

    public void setContent(String content) {
        this.content = content;
    }

    public String getWriter() {
        return writer;
    }

    public void setWriter(String writer) {
        this.writer = writer;
    }

    public LocalDateTime getCreatedAt() {
        return createdAt;
    }

    public void setCreatedAt(LocalDateTime createdAt) {
        this.createdAt = createdAt;
    }

    public LocalDateTime getUpdatedAt() {
        return updatedAt;
    }

    public void setUpdatedAt(LocalDateTime updatedAt) {
        this.updatedAt = updatedAt;
    }
}



7. BoardDAO.java

이제 핵심인 DAO입니다.

CRUD를 모두 구현합니다.

create()
findAll()
findById()
update()
delete()

파일:

src/main/java/com/mobility/git/project2/dao/BoardDAO.java
package com.mobility.git.project2.dao;

import com.mobility.git.project2.dto.BoardDTO;
import com.mobility.git.project2.util.DBUtil;

import java.sql.*;
import java.util.ArrayList;
import java.util.List;

public class BoardDAO {

    // 게시글 등록
    public long insert(BoardDTO board) {

        String sql = """
                INSERT INTO board (title, content, writer)
                VALUES (?, ?, ?)
                RETURNING id
                """;

        try (
                Connection conn = DBUtil.getConnection();
                PreparedStatement pstmt = conn.prepareStatement(sql)
        ) {

            pstmt.setString(1, board.getTitle());
            pstmt.setString(2, board.getContent());
            pstmt.setString(3, board.getWriter());

            try (ResultSet rs = pstmt.executeQuery()) {

                if (rs.next()) {
                    return rs.getLong("id");
                }
            }

        } catch (SQLException e) {
            throw new RuntimeException("게시글 등록 중 오류가 발생했습니다.", e);
        }

        return 0;
    }


    // 게시글 전체 조회
    public List<BoardDTO> findAll() {

        String sql = """
                SELECT id,
                       title,
                       content,
                       writer,
                       created_at,
                       updated_at
                FROM board
                ORDER BY id DESC
                """;

        List<BoardDTO> boardList = new ArrayList<>();

        try (
                Connection conn = DBUtil.getConnection();
                PreparedStatement pstmt = conn.prepareStatement(sql);
                ResultSet rs = pstmt.executeQuery()
        ) {

            while (rs.next()) {

                BoardDTO board = mapRow(rs);

                boardList.add(board);
            }

        } catch (SQLException e) {
            throw new RuntimeException("게시글 목록 조회 중 오류가 발생했습니다.", e);
        }

        return boardList;
    }


    // 게시글 상세 조회
    public BoardDTO findById(long id) {

        String sql = """
                SELECT id,
                       title,
                       content,
                       writer,
                       created_at,
                       updated_at
                FROM board
                WHERE id = ?
                """;

        try (
                Connection conn = DBUtil.getConnection();
                PreparedStatement pstmt = conn.prepareStatement(sql)
        ) {

            pstmt.setLong(1, id);

            try (ResultSet rs = pstmt.executeQuery()) {

                if (rs.next()) {
                    return mapRow(rs);
                }
            }

        } catch (SQLException e) {
            throw new RuntimeException("게시글 조회 중 오류가 발생했습니다.", e);
        }

        return null;
    }


    // 게시글 수정
    public boolean update(BoardDTO board) {

        String sql = """
                UPDATE board
                SET title = ?,
                    content = ?,
                    writer = ?,
                    updated_at = CURRENT_TIMESTAMP
                WHERE id = ?
                """;

        try (
                Connection conn = DBUtil.getConnection();
                PreparedStatement pstmt = conn.prepareStatement(sql)
        ) {

            pstmt.setString(1, board.getTitle());
            pstmt.setString(2, board.getContent());
            pstmt.setString(3, board.getWriter());
            pstmt.setLong(4, board.getId());

            return pstmt.executeUpdate() > 0;

        } catch (SQLException e) {
            throw new RuntimeException("게시글 수정 중 오류가 발생했습니다.", e);
        }
    }


    // 게시글 삭제
    public boolean delete(long id) {

        String sql = """
                DELETE FROM board
                WHERE id = ?
                """;

        try (
                Connection conn = DBUtil.getConnection();
                PreparedStatement pstmt = conn.prepareStatement(sql)
        ) {

            pstmt.setLong(1, id);

            return pstmt.executeUpdate() > 0;

        } catch (SQLException e) {
            throw new RuntimeException("게시글 삭제 중 오류가 발생했습니다.", e);
        }
    }


    // ResultSet → DTO 변환
    private BoardDTO mapRow(ResultSet rs) throws SQLException {

        BoardDTO board = new BoardDTO();

        board.setId(rs.getLong("id"));
        board.setTitle(rs.getString("title"));
        board.setContent(rs.getString("content"));
        board.setWriter(rs.getString("writer"));

        Timestamp createdAt = rs.getTimestamp("created_at");
        Timestamp updatedAt = rs.getTimestamp("updated_at");

        if (createdAt != null) {
            board.setCreatedAt(createdAt.toLocalDateTime());
        }

        if (updatedAt != null) {
            board.setUpdatedAt(updatedAt.toLocalDateTime());
        }

        return board;
    }
}

여기서 중요한 것은 SQL Injection 방지를 위해 전부:

PreparedStatement

를 사용했다는 것입니다.

예를 들어:

pstmt.setLong(1, id);

처럼 parameter binding을 사용합니다.




8. BoardController.java

이제 MVC Level 2의 핵심입니다.

하나의 Front Controller가 /board/* 요청을 받아서 작업을 분기합니다.

파일:

src/main/java/com/mobility/git/project2/controller/BoardController.java
package com.mobility.git.project2.controller;

import com.mobility.git.project2.dao.BoardDAO;
import com.mobility.git.project2.dto.BoardDTO;
import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.HttpServlet;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;

import java.io.IOException;
import java.util.List;

@WebServlet("/board/*")
public class BoardController extends HttpServlet {

    private BoardDAO boardDAO;

    @Override
    public void init() {
        boardDAO = new BoardDAO();
    }


    @Override
    protected void doGet(
            HttpServletRequest request,
            HttpServletResponse response
    ) throws ServletException, IOException {

        String path = request.getPathInfo();

        if (path == null || "/".equals(path)) {
            list(request, response);
            return;
        }

        switch (path) {

            case "/list":
                list(request, response);
                break;

            case "/detail":
                detail(request, response);
                break;

            case "/write":
                writeForm(request, response);
                break;

            case "/edit":
                editForm(request, response);
                break;

            default:
                response.sendError(
                        HttpServletResponse.SC_NOT_FOUND
                );
        }
    }


    @Override
    protected void doPost(
            HttpServletRequest request,
            HttpServletResponse response
    ) throws ServletException, IOException {

        request.setCharacterEncoding("UTF-8");

        String path = request.getPathInfo();

        if (path == null) {
            response.sendError(
                    HttpServletResponse.SC_NOT_FOUND
            );
            return;
        }

        switch (path) {

            case "/write":
                insert(request, response);
                break;

            case "/edit":
                update(request, response);
                break;

            case "/delete":
                delete(request, response);
                break;

            default:
                response.sendError(
                        HttpServletResponse.SC_NOT_FOUND
                );
        }
    }


    // ================================
    // 게시글 목록
    // GET /board/list
    // ================================
    private void list(
            HttpServletRequest request,
            HttpServletResponse response
    ) throws ServletException, IOException {

        List<BoardDTO> boardList = boardDAO.findAll();

        request.setAttribute("boardList", boardList);

        request.getRequestDispatcher(
                "/WEB-INF/views/board/list.jsp"
        ).forward(request, response);
    }


    // ================================
    // 게시글 상세
    // GET /board/detail?id=1
    // ================================
    private void detail(
            HttpServletRequest request,
            HttpServletResponse response
    ) throws ServletException, IOException {

        long id = getId(request);

        BoardDTO board = boardDAO.findById(id);

        if (board == null) {
            response.sendError(
                    HttpServletResponse.SC_NOT_FOUND,
                    "게시글을 찾을 수 없습니다."
            );
            return;
        }

        request.setAttribute("board", board);

        request.getRequestDispatcher(
                "/WEB-INF/views/board/detail.jsp"
        ).forward(request, response);
    }


    // ================================
    // 게시글 등록 화면
    // GET /board/write
    // ================================
    private void writeForm(
            HttpServletRequest request,
            HttpServletResponse response
    ) throws ServletException, IOException {

        request.getRequestDispatcher(
                "/WEB-INF/views/board/write.jsp"
        ).forward(request, response);
    }


    // ================================
    // 게시글 등록 처리
    // POST /board/write
    // ================================
    private void insert(
            HttpServletRequest request,
            HttpServletResponse response
    ) throws IOException {

        String title = request.getParameter("title");
        String content = request.getParameter("content");
        String writer = request.getParameter("writer");

        BoardDTO board = new BoardDTO();

        board.setTitle(title);
        board.setContent(content);
        board.setWriter(writer);

        long id = boardDAO.insert(board);

        response.sendRedirect(
                request.getContextPath()
                        + "/board/detail?id="
                        + id
        );
    }


    // ================================
    // 게시글 수정 화면
    // GET /board/edit?id=1
    // ================================
    private void editForm(
            HttpServletRequest request,
            HttpServletResponse response
    ) throws ServletException, IOException {

        long id = getId(request);

        BoardDTO board = boardDAO.findById(id);

        if (board == null) {
            response.sendError(
                    HttpServletResponse.SC_NOT_FOUND,
                    "게시글을 찾을 수 없습니다."
            );
            return;
        }

        request.setAttribute("board", board);

        request.getRequestDispatcher(
                "/WEB-INF/views/board/edit.jsp"
        ).forward(request, response);
    }


    // ================================
    // 게시글 수정 처리
    // POST /board/edit
    // ================================
    private void update(
            HttpServletRequest request,
            HttpServletResponse response
    ) throws IOException {

        long id = getId(request);

        String title = request.getParameter("title");
        String content = request.getParameter("content");
        String writer = request.getParameter("writer");

        BoardDTO board = new BoardDTO();

        board.setId(id);
        board.setTitle(title);
        board.setContent(content);
        board.setWriter(writer);

        boardDAO.update(board);

        response.sendRedirect(
                request.getContextPath()
                        + "/board/detail?id="
                        + id
        );
    }


    // ================================
    // 게시글 삭제
    // POST /board/delete
    // ================================
    private void delete(
            HttpServletRequest request,
            HttpServletResponse response
    ) throws IOException {

        long id = getId(request);

        boardDAO.delete(id);

        response.sendRedirect(
                request.getContextPath()
                        + "/board/list"
        );
    }


    // ================================
    // id 파라미터 변환
    // ================================
    private long getId(
            HttpServletRequest request
    ) throws IOException {

        String idParameter = request.getParameter("id");

        if (idParameter == null || idParameter.isBlank()) {
            throw new IllegalArgumentException(
                    "id 파라미터가 없습니다."
            );
        }

        try {

            return Long.parseLong(idParameter);

        } catch (NumberFormatException e) {

            throw new IllegalArgumentException(
                    "id가 올바르지 않습니다."
            );
        }
    }
}

이 Servlet 하나가 Controller 역할을 합니다.

예를 들어:

GET /board/list
        ↓
BoardController
        ↓
BoardDAO.findAll()
        ↓
list.jsp



9. web.xml

파일:

src/main/webapp/WEB-INF/web.xml
<?xml version="1.0" encoding="UTF-8"?>

<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="
            https://jakarta.ee/xml/ns/jakartaee
            https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>Board MVC</display-name>

    <!-- UTF-8 Encoding Filter -->
    <filter>
        <filter-name>encodingFilter</filter-name>
        <filter-class>
            org.apache.catalina.filters.SetCharacterEncodingFilter
        </filter-class>

        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>

        <init-param>
            <param-name>ignore</param-name>
            <param-value>false</param-value>
        </init-param>
    </filter>

    <filter-mapping>
        <filter-name>encodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

</web-app>

Tomcat 10.1의 Servlet API는 Jakarta Servlet 6.0 기반입니다. 따라서 web.xml도 Jakarta EE 6.0 네임스페이스를 사용합니다.




10. index.jsp

프로젝트 시작 페이지입니다.

파일:

src/main/webapp/index.jsp
<%@ page contentType="text/html; charset=UTF-8" pageEncoding="UTF-8" %>
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Project2</title>

    <!-- Bootstrap 5 -->
    <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
            rel="stylesheet">
</head>

<body>

<div class="container py-5">

    <div class="text-center">

        <h1 class="fw-bold mb-3">
            Hello~! JSP
        </h1>

        <p class="text-muted mb-4">
            Tomcat 10.1 + JSP 테스트 성공!
        </p>

        <!-- 게시글 목록 이동 -->
        <a href="${pageContext.request.contextPath}/list"
           class="btn btn-primary">
            게시글 목록
        </a>

    </div>

</div>

<!-- Bootstrap JS -->
<script
        src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js">
</script>

</body>
</html>

브라우저에서:

http://localhost:8080/board-mvc/

라고 들어오면:

index.jsp
   ↓
/board/list
   ↓
BoardController

가 됩니다.




11. list.jsp

게시글 목록입니다.

파일:

src/main/webapp/WEB-INF/views/board/list.jsp
<%@ page contentType="text/html; charset=UTF-8"
         pageEncoding="UTF-8" %>

<%@ taglib prefix="c"
           uri="jakarta.tags.core" %>

<!DOCTYPE html>
<html lang="ko">

<head>

    <meta charset="UTF-8">

    <meta name="viewport"
          content="width=device-width, initial-scale=1">

    <title>게시판</title>

    <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
        rel="stylesheet"
        integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
        crossorigin="anonymous">

    <link
        rel="stylesheet"
        href="${pageContext.request.contextPath}/css/board.css">

</head>

<body>

<div class="container mt-5">

    <div class="d-flex justify-content-between align-items-center mb-4">

        <h1>게시판</h1>

        <a
            href="${pageContext.request.contextPath}/board/write"
            class="btn btn-primary">

            게시글 등록

        </a>

    </div>


    <div class="card">

        <div class="card-body">

            <div class="table-responsive">

                <table class="table table-hover align-middle">

                    <thead class="table-light">

                    <tr>

                        <th style="width: 10%">번호</th>

                        <th>제목</th>

                        <th style="width: 15%">작성자</th>

                        <th style="width: 20%">작성일</th>

                    </tr>

                    </thead>

                    <tbody>

                    <c:choose>

                        <c:when test="${empty boardList}">

                            <tr>

                                <td
                                    colspan="4"
                                    class="text-center py-5">

                                    등록된 게시글이 없습니다.

                                </td>

                            </tr>

                        </c:when>


                        <c:otherwise>

                            <c:forEach
                                var="board"
                                items="${boardList}">

                                <tr>

                                    <td>
                                        <c:out value="${board.id}"/>
                                    </td>

                                    <td>

                                        <a
                                            href="${pageContext.request.contextPath}/board/detail?id=${board.id}"
                                            class="text-decoration-none">

                                            <c:out value="${board.title}"/>

                                        </a>

                                    </td>

                                    <td>
                                        <c:out value="${board.writer}"/>
                                    </td>

                                    <td>
                                        <c:out value="${board.createdAt}"/>
                                    </td>

                                </tr>

                            </c:forEach>

                        </c:otherwise>

                    </c:choose>

                    </tbody>

                </table>

            </div>

        </div>

    </div>

</div>


<script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
    crossorigin="anonymous">
</script>

</body>

</html>

Bootstrap 공식 문서에서도 5.3.8 CSS/JS를 jsDelivr CDN으로 사용하는 방식을 안내하고 있습니다.




12. detail.jsp

게시글 상세보기입니다.

파일:

src/main/webapp/WEB-INF/views/board/detail.jsp
<%@ page contentType="text/html; charset=UTF-8"
         pageEncoding="UTF-8" %>

<%@ taglib prefix="c"
           uri="jakarta.tags.core" %>

<!DOCTYPE html>
<html lang="ko">

<head>

    <meta charset="UTF-8">

    <meta name="viewport"
          content="width=device-width, initial-scale=1">

    <title>게시글 상세</title>

    <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
        rel="stylesheet"
        integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
        crossorigin="anonymous">

    <link
        rel="stylesheet"
        href="${pageContext.request.contextPath}/css/board.css">

</head>

<body>

<div class="container mt-5">

    <div class="d-flex justify-content-between align-items-center mb-4">

        <h1>게시글 상세</h1>

        <a
            href="${pageContext.request.contextPath}/board/list"
            class="btn btn-outline-secondary">

            목록

        </a>

    </div>


    <div class="card">

        <div class="card-header">

            <h3 class="mb-0">
                <c:out value="${board.title}"/>
            </h3>

        </div>


        <div class="card-body">

            <div class="row mb-3">

                <div class="col-md-6">

                    <strong>작성자</strong>

                    <span class="ms-2">
                        <c:out value="${board.writer}"/>
                    </span>

                </div>

                <div class="col-md-6">

                    <strong>작성일</strong>

                    <span class="ms-2">
                        <c:out value="${board.createdAt}"/>
                    </span>

                </div>

            </div>


            <hr>


            <div class="board-content">

                <c:out value="${board.content}"/>

            </div>

        </div>


        <div class="card-footer d-flex justify-content-end gap-2">

            <a
                href="${pageContext.request.contextPath}/board/edit?id=${board.id}"
                class="btn btn-warning">

                수정

            </a>


            <form
                action="${pageContext.request.contextPath}/board/delete"
                method="post"
                onsubmit="return confirm('정말 삭제하시겠습니까?');">

                <input
                    type="hidden"
                    name="id"
                    value="${board.id}">

                <button
                    type="submit"
                    class="btn btn-danger">

                    삭제

                </button>

            </form>


            <a
                href="${pageContext.request.contextPath}/board/list"
                class="btn btn-secondary">

                목록

            </a>

        </div>

    </div>

</div>


<script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-FKyoEForCGlyvwx9HJcYn3nv7wiPVlz7YYjR4wJrWVcXK/BmnVDxM+D2scQbITxI"
    crossorigin="anonymous">
</script>

</body>

</html>

여기서 중요한 것이:

<c:out value="${board.content}"/>

입니다.

그냥:

${board.content}

라고 출력하는 것보다 HTML 특수문자 escaping을 적용할 수 있어서 게시판 실습에서 더 안전한 형태입니다.




13. write.jsp

게시글 등록 화면입니다.

파일:

src/main/webapp/WEB-INF/views/board/write.jsp
<%@ page contentType="text/html; charset=UTF-8"
         pageEncoding="UTF-8" %>

<!DOCTYPE html>
<html lang="ko">

<head>

    <meta charset="UTF-8">

    <meta name="viewport"
          content="width=device-width, initial-scale=1">

    <title>게시글 등록</title>

    <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
        rel="stylesheet"
        integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
        crossorigin="anonymous">

</head>

<body>

<div class="container mt-5">

    <div class="d-flex justify-content-between align-items-center mb-4">

        <h1>게시글 등록</h1>

        <a
            href="${pageContext.request.contextPath}/board/list"
            class="btn btn-outline-secondary">

            목록

        </a>

    </div>


    <div class="card">

        <div class="card-body">

            <form
                action="${pageContext.request.contextPath}/board/write"
                method="post">


                <!-- 제목 -->

                <div class="mb-3">

                    <label
                        for="title"
                        class="form-label">

                        제목

                    </label>

                    <input
                        type="text"
                        class="form-control"
                        id="title"
                        name="title"
                        maxlength="200"
                        required>

                </div>


                <!-- 작성자 -->

                <div class="mb-3">

                    <label
                        for="writer"
                        class="form-label">

                        작성자

                    </label>

                    <input
                        type="text"
                        class="form-control"
                        id="writer"
                        name="writer"
                        maxlength="100"
                        required>

                </div>


                <!-- 내용 -->

                <div class="mb-3">

                    <label
                        for="content"
                        class="form-label">

                        내용

                    </label>

                    <textarea
                        class="form-control"
                        id="content"
                        name="content"
                        rows="10"
                        required></textarea>

                </div>


                <!-- 버튼 -->

                <div class="d-flex justify-content-end gap-2">

                    <a
                        href="${pageContext.request.contextPath}/board/list"
                        class="btn btn-secondary">

                        취소

                    </a>

                    <button
                        type="submit"
                        class="btn btn-primary">

                        등록

                    </button>

                </div>

            </form>

        </div>

    </div>

</div>


<script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYjWrWVcXK/BmnVDxM+D2scQbITxI"
    crossorigin="anonymous">
</script>

</body>

</html>



14. edit.jsp

게시글 수정 화면입니다.

파일:

src/main/webapp/WEB-INF/views/board/edit.jsp
<%@ page contentType="text/html; charset=UTF-8"
         pageEncoding="UTF-8" %>

<%@ taglib prefix="c"
           uri="jakarta.tags.core" %>

<!DOCTYPE html>
<html lang="ko">

<head>

    <meta charset="UTF-8">

    <meta name="viewport"
          content="width=device-width, initial-scale=1">

    <title>게시글 수정</title>

    <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css"
        rel="stylesheet"
        integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB"
        crossorigin="anonymous">

</head>

<body>

<div class="container mt-5">

    <div class="d-flex justify-content-between align-items-center mb-4">

        <h1>게시글 수정</h1>

        <a
            href="${pageContext.request.contextPath}/board/list"
            class="btn btn-outline-secondary">

            목록

        </a>

    </div>


    <div class="card">

        <div class="card-body">

            <form
                action="${pageContext.request.contextPath}/board/edit"
                method="post">


                <input
                    type="hidden"
                    name="id"
                    value="${board.id}">


                <!-- 제목 -->

                <div class="mb-3">

                    <label
                        for="title"
                        class="form-label">

                        제목

                    </label>

                    <input
                        type="text"
                        class="form-control"
                        id="title"
                        name="title"
                        value="<c:out value='${board.title}'/>"
                        maxlength="200"
                        required>

                </div>


                <!-- 작성자 -->

                <div class="mb-3">

                    <label
                        for="writer"
                        class="form-label">

                        작성자

                    </label>

                    <input
                        type="text"
                        class="form-control"
                        id="writer"
                        name="writer"
                        value="<c:out value='${board.writer}'/>"
                        maxlength="100"
                        required>

                </div>


                <!-- 내용 -->

                <div class="mb-3">

                    <label
                        for="content"
                        class="form-label">

                        내용

                    </label>

                    <textarea
                        class="form-control"
                        id="content"
                        name="content"
                        rows="10"
                        required><c:out value="${board.content}"/></textarea>

                </div>


                <!-- 버튼 -->

                <div class="d-flex justify-content-end gap-2">

                    <a
                        href="${pageContext.request.contextPath}/board/detail?id=${board.id}"
                        class="btn btn-secondary">

                        취소

                    </a>

                    <button
                        type="submit"
                        class="btn btn-warning">

                        수정

                    </button>

                </div>

            </form>

        </div>

    </div>

</div>


<script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-FKyoEForCGlyvwx9HJcYn3nv7wiPVlz7YYjWrWVcXK/BmnVDxM+D2scQbITxI"
    crossorigin="anonymous">
</script>

</body>

</html>



15. board.css

Bootstrap만으로도 충분하지만, 우리가 직접 만든 CSS도 하나 넣겠습니다.

파일:

src/main/webapp/css/board.css
body {
    background-color: #f8f9fa;
}

.card {
    border-radius: 10px;
}

.board-content {
    min-height: 300px;
    white-space: pre-wrap;
    line-height: 1.8;
}

.table td,
.table th {
    vertical-align: middle;
}

.table a {
    color: #212529;
}

.table a:hover {
    text-decoration: underline !important;
}



16. URL 구조

이제 우리가 만든 게시판의 URL은 다음과 같습니다.

기능	HTTP	URL
목록	GET	/board/list
상세	GET	/board/detail?id=1
등록 화면	GET	/board/write
등록 처리	POST	/board/write
수정 화면	GET	/board/edit?id=1
수정 처리	POST	/board/edit
삭제 처리	POST	/board/delete

즉:

/board/list
       ↓
목록

/board/detail?id=1
       ↓
상세

/board/write
       ↓
등록 화면

POST /board/write
       ↓
등록 처리

/board/edit?id=1
       ↓
수정 화면

POST /board/edit
       ↓
수정 처리

POST /board/delete
       ↓
삭제

입니다.



